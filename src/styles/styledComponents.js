import styled from 'styled-components';
import { colors } from './variables';

export const Container = styled.div`
  width: 100%;
  color: ${colors.text};
  max-width: 1200px;
  margin: 0 auto;
  padding: 1rem;
`;
